# The-Powder-Toy (C++ engine + Lua RPG) — router

This tree is the engine and the tree the game loads from. The MCP server, knowledge base, skills and
all process rules live in `D:\powder-toy` — read `D:\powder-toy\CLAUDE.md` first and load its skills
from `D:\powder-toy\.claude\skills\` (in particular `powder-knowledge` for any element/reaction/
process/machine question, `tpt-engine-constraints` and `process-and-bridge-ops` before touching
C++, the bridge, or a running game). Never touch the live game on port 9876; lab is 9877.
